# EXAMPLE ONLY — do not apply with real passwords committed.
# kubectl create secret generic bugsathi-secrets -n bugsathi \
#   --from-literal=POSTGRES_PASSWORD='...' \
#   --from-literal=MINIO_ACCESS_KEY='...' \
#   --from-literal=MINIO_SECRET_KEY='...' \
#   --from-literal=JWT_SECRET='...'
apiVersion: v1
kind: Secret
metadata:
  name: bugsathi-secrets
  namespace: bugsathi
type: Opaque
stringData:
  POSTGRES_PASSWORD: "replace-me"
  MINIO_ACCESS_KEY: "replace-me"
  MINIO_SECRET_KEY: "replace-me"
  JWT_SECRET: "replace-me-must-be-at-least-32-characters"
